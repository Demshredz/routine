# Change History (Log)

*Dieses Log wird nach JEDER relevanten Änderung im Projekt aktualisiert.*

| Datum | Änderung | Grund | Betroffene Dateien | Seiteneffekte / ToDos |
| :--- | :--- | :--- | :--- | :--- |
| **2026-04-01** | Final Polish: Gamification & UX (Phase F, G, H) | Massive Steigerung des Dopamin-Loops durch Leveling, Sound-Effects, Swipe-Gesten und Daten-Korrelation. | `index.tsx`, `explore.tsx`, `RoutineCard.tsx`, `TimelineCard.tsx`, `useRoutineStore.ts`, `utils/audio.ts` | `expo-av` für Sound hinzugefügt. `Swipeable` implementiert (Right Swipe = Done). App hat jetzt EXP, Level und ein Feier-Overlay für Level-Ups. Im Statistiken Tab wird die Korrelation von Energielevel und Erledigungs-Rate (KI Insight) angezeigt.<br> **Tech Decision:** `react-native-gesture-handler/Swipeable` statt Reanimated Custom-Drag genutzt für native 60fps Performance. `expo-av` im `_layout` pre-loaded (singleton) für Caching & Zero-Latency Audio. KI-Insight hart auf Store-Values `completions` u. `moods` On-the-Fly bei Render berechnet (Menoized), keine DB Speicherung nötig für Historiengrad < 365. |
| **2026-04-01** | AI Coach OpenAI Integration (Phase E) | Fake-Logik entfernt, stattdessen echter intelligenter Routinen-Generator via OpenAI. | `utils/openai.ts`, `ai-coach.tsx`, `.env`, `.gitignore` | `gpt-4o-mini` API Key lokal in `.env` hinterlegt. Coach berücksichtigt bestehende Routinen und gibt strukturiertes JSON (inkl. Icons, Zeiten, Farben) zurück, das direkt anwendbar ist.<br> **Tech Decision:** GPT-4o-mini ohne SDK rein über Web `fetch` API angesprochen, limitiert den Build-Verbrauch (- npm size) und zwingt Output via `response_format: { type: 'json_object' }` in Typ-Sicherheit für Expo-Store Ingestion. UI greift Moti für die Chat-Preview auf. |
| **2026-04-01** | Push Notifications & Drag & Drop (Phase D) | Echte Sperrbildschirm-Reminder und manuelle Listenerstellung stärken den Produktivitäts-Loop | `utils/notifications.ts`, `_layout.tsx`, `index.tsx`, `useRoutineStore.ts` | `expo-notifications` und `react-native-draggable-flatlist` installiert. `index.tsx` auf `DraggableFlatList` refactored. Routine Model in Store wurde um `notificationId` ergänzt, sodass Erinnerungen bei Löschung der Routine ebenfalls gecanceled werden.<br> **Tech Decision:** Local Push Trigger statt Push-Server Backend um API/Backend-Kosten zu sparen und App Standalone 100% offline fähig zu halten. Android-Channel Registration in `_layout.tsx` um Cold-Start Delivery Issues in Expo 54 zu vermeiden. Drag-Drop (DFL) auf List-Tab beschränkt wegen ScrollView Konflikten in Timeline-Ansicht. |
| **2026-04-01** | Interactive Calendar (Phase C) | Navigation von vergangenen und zukünftigen Tagen ermöglicht | `useRoutineStore.ts`, `calendar.tsx`, `MonthlyCalendar.tsx`, `index.tsx`, `_layout.tsx` | Community Tab gelöscht. Store um `selectedDate` erweitert. Home Tab liest jetzt `selectedDate` statt fixem "Heute".<br> **Tech Decision:** Globaler Zustand (`selectedDate`) eingeführt, damit Home/Timeline dynamisch triggern ohne Parameter passing in URL-Routing. |
| **2026-04-01** | Premium Routine Creator (Phase B) | Erweiterung der Routine-Konfiguration um Icons, Farben und Wiederholungen | `IconPicker.tsx`, `ColorPicker.tsx`, `index.tsx` | UI für Icon Grid und Farb-Auswahl integriert. Routine Model in Store wurde entsprechend bespielt. |
| **2026-03-31** | Timeline View (Phase A) implementiert | Structured-Killer Feature: Visueller Tagesablauf mit Uhrzeiten, Now-Indicator, Accent Bars, Icons | `useRoutineStore.ts`, `TimelineView.tsx`, `TimelineCard.tsx`, `NowIndicator.tsx`, `index.tsx` | Store Version erhöhen (`routine-storage-v8`), Routine Interface erweitert (time, endTime, duration, icon, color, recurrence). View Toggle (List/Timeline) auf Home. |
| **2026-03-31** | Android Testing & UX Phase 1 (Data-Fix & Theme-Refactoring) | Verhinderung von UX/Data-Glitches auf Android | `store/useRoutineStore.ts`, `hooks/use-app-theme.ts`, `app/(tabs)/*` | `pruneData` auf 365 Tage verlängert. Zeitzonen Streak-Fix integriert. Theme-Config konsolidiert. Android Bottom-Tab Style integriert. |
| **2026-03-31** | Etablierung strenger "Brain-First" Architekten-Regeln. | Sicherstellung von technischer Produktqualität & 15-Min-Onboarding. | - | Zukünftiger Workflow: Analyse -> Brain Check -> Code Edit -> Brain Sync. |
| **2026-03-31** | Initiales Brain Setup. | Projektübergabe für Agentic Coding. | Alle `/Brain` Dateien | Projekt analysiert. Status Quo dokumentiert. |
